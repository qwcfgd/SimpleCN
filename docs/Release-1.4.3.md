# SimpleCN V1.4.3

- 程序窗口标题改为 `SimpleCN`。
- 主页标题改为 `SimpleCN V1.4.3`，标语改为 `SIMPLE CONTROLLER FOR CAN/LIN`。
- 同步应用版本、Windows 文件版本与发布包版本信息为 1.4.3。上述品牌文本在简中 / Eng 下保持一致。
- 包含已完成的秒单位、完整报文缓存、暂停和历史浏览更新，详见 [报文监视](Trace-and-Graphics.md)。

工程及构建目录沿用既定路径，程序文件名保持 `QtBootloader.exe`。本次仅更新品牌及版本信息，沿用现有语言偏好配置。

验证：Qt 5.15.19 / Qt 6.8.4 构建完成，两套现有主窗口布局检查及本地化回归通过；Windows 文件属性均为 SimpleCN、1.4.3（文件版本 1.4.3.0）。
