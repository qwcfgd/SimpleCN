add_executable(test_communication test_communication.cpp)
target_link_libraries(test_communication PRIVATE peak_communication Qt${QT_VERSION_MAJOR}::Test)
add_test(NAME communication COMMAND test_communication -o "${CMAKE_CURRENT_BINARY_DIR}/communication-results.xml,xml")
add_executable(plin_smoke plin_smoke.cpp)
target_link_libraries(plin_smoke PRIVATE peak_communication)
add_executable(plin_scan plin_scan.cpp)
target_link_libraries(plin_scan PRIVATE peak_communication)
peak_deploy(plin_scan)
peak_deploy(test_communication)
peak_deploy(plin_smoke)
get_target_property(qmake_executable Qt${QT_VERSION_MAJOR}::qmake IMPORTED_LOCATION)
get_filename_component(qt_bin "${qmake_executable}" DIRECTORY)
find_program(WINDEPLOYQT_EXECUTABLE windeployqt HINTS "${qt_bin}" NO_DEFAULT_PATH)
if(WINDEPLOYQT_EXECUTABLE)
    foreach(target test_communication plin_smoke plin_scan)
        add_custom_command(TARGET ${target} POST_BUILD
            COMMAND "${WINDEPLOYQT_EXECUTABLE}" --no-translations --no-compiler-runtime "$<TARGET_FILE:${target}>")
    endforeach()
endif()

add_executable(test_host_ui test_host_ui.cpp)
target_link_libraries(test_host_ui PRIVATE bootloader_ui Qt${QT_VERSION_MAJOR}::Test)
host_deploy(test_host_ui)
add_custom_command(TARGET test_host_ui POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E copy_if_different "${qt_bin}/../plugins/platforms/qoffscreen.dll" "$<TARGET_FILE_DIR:test_host_ui>/platforms/qoffscreen.dll")
add_test(NAME host_ui COMMAND test_host_ui -o "${CMAKE_CURRENT_BINARY_DIR}/host-ui-results.xml,xml")
set_tests_properties(host_ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=windows" TIMEOUT 60)

add_executable(test_protocol test_protocol.cpp)
target_link_libraries(test_protocol PRIVATE bootloader_protocol Qt${QT_VERSION_MAJOR}::Test)
host_deploy(test_protocol)
add_test(NAME protocol COMMAND test_protocol -o "${CMAKE_CURRENT_BINARY_DIR}/protocol-results.xml,xml")
set_tests_properties(protocol PROPERTIES TIMEOUT 90)

add_executable(test_can_protocol test_can_protocol.cpp)
target_link_libraries(test_can_protocol PRIVATE bootloader_protocol Qt${QT_VERSION_MAJOR}::Test)
host_deploy(test_can_protocol)
add_test(NAME can_protocol COMMAND test_can_protocol -o "${CMAKE_CURRENT_BINARY_DIR}/can-protocol-results.xml,xml")
set_tests_properties(can_protocol PROPERTIES TIMEOUT 90)

# Verifier and simulation fixtures remain in qttemp; the application stays clean.
file(COPY "${CMAKE_SOURCE_DIR}/profiles/fixtures" DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/profiles")
configure_file("${CMAKE_SOURCE_DIR}/profiles/stage6-can-lin-simulation.json" "${CMAKE_CURRENT_BINARY_DIR}/profiles/simulation.json" COPYONLY)
add_executable(VerifyRelease release_smoke.cpp)
target_link_libraries(VerifyRelease PRIVATE bootloader_ui Qt${QT_VERSION_MAJOR}::Test)
target_compile_definitions(VerifyRelease PRIVATE HOST_APP_RUNTIME_DIR="${HOST_RUNTIME_OUTPUT_DIR}")
add_test(NAME release_runtime COMMAND VerifyRelease -o "${CMAKE_CURRENT_BINARY_DIR}/release-runtime-results.xml,xml")
set_tests_properties(release_runtime PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=windows" TIMEOUT 60)
host_deploy(VerifyRelease)

add_executable(test_download_revision test_download_revision.cpp)
target_link_libraries(test_download_revision PRIVATE bootloader_ui Qt${QT_VERSION_MAJOR}::Test)
host_deploy(test_download_revision)
add_test(NAME download_revision COMMAND test_download_revision -o "${CMAKE_CURRENT_BINARY_DIR}/download-revision-results.xml,xml")
set_tests_properties(download_revision PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=windows" TIMEOUT 90)

add_executable(test_cdd test_cdd.cpp)
target_link_libraries(test_cdd PRIVATE cdd_model Qt${QT_VERSION_MAJOR}::Test)
target_compile_definitions(test_cdd PRIVATE TEST_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
host_deploy(test_cdd)
add_test(NAME cdd COMMAND test_cdd -o "${CMAKE_CURRENT_BINARY_DIR}/cdd-results.xml,xml")
set_tests_properties(cdd PROPERTIES TIMEOUT 30)
add_executable(test_uds_ui test_uds_ui.cpp)
target_link_libraries(test_uds_ui PRIVATE bootloader_ui Qt${QT_VERSION_MAJOR}::Test)
target_compile_definitions(test_uds_ui PRIVATE TEST_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
host_deploy(test_uds_ui)
add_test(NAME uds_ui COMMAND test_uds_ui -o "${CMAKE_CURRENT_BINARY_DIR}/uds-ui-results.xml,xml")
set_tests_properties(uds_ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=windows" TIMEOUT 60)

# Native-only unit and simulation acceptance tests; no physical bus access.
add_executable(test_signal_codec test_signal_codec.cpp)
add_executable(test_signal_driver test_signal_driver.cpp)
target_link_libraries(test_signal_driver PRIVATE peak_communication Qt${QT_VERSION_MAJOR}::Test)
host_deploy(test_signal_driver)
add_test(NAME signal_driver COMMAND test_signal_driver -o "${CMAKE_CURRENT_BINARY_DIR}/signal-driver-results.xml,xml")
set_tests_properties(signal_driver PROPERTIES TIMEOUT 30)
target_link_libraries(test_signal_codec PRIVATE signal_runtime Qt${QT_VERSION_MAJOR}::Test)
target_compile_definitions(test_signal_codec PRIVATE TEST_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
host_deploy(test_signal_codec)
add_test(NAME signal_codec COMMAND test_signal_codec -o "${CMAKE_CURRENT_BINARY_DIR}/signal-codec-results.xml,xml")
set_tests_properties(signal_codec PROPERTIES TIMEOUT 60)
add_executable(test_signal_ui test_signal_ui.cpp)
target_link_libraries(test_signal_ui PRIVATE bootloader_ui Qt${QT_VERSION_MAJOR}::Test)
target_compile_definitions(test_signal_ui PRIVATE TEST_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
host_deploy(test_signal_ui)
add_custom_command(TARGET test_signal_ui POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E make_directory "$<TARGET_FILE_DIR:test_signal_ui>/platforms"
    COMMAND ${CMAKE_COMMAND} -E copy_if_different "${qt_bin}/../plugins/platforms/qoffscreen.dll" "$<TARGET_FILE_DIR:test_signal_ui>/platforms/qoffscreen.dll")
add_test(NAME signal_ui COMMAND test_signal_ui -o "${CMAKE_CURRENT_BINARY_DIR}/signal-ui-results.xml,xml")
set_tests_properties(signal_ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60)

add_executable(signal_fixture_probe signal_fixture_probe.cpp)
target_link_libraries(signal_fixture_probe PRIVATE signal_database)
host_deploy(signal_fixture_probe)

# Trace, export and graphics regression tests; no physical bus access.
add_executable(test_trace_plot test_trace_plot.cpp)
target_link_libraries(test_trace_plot PRIVATE bootloader_ui Qt${QT_VERSION_MAJOR}::Test)
target_compile_definitions(test_trace_plot PRIVATE TEST_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
host_deploy(test_trace_plot)
add_test(NAME trace_plot COMMAND test_trace_plot -o "${CMAKE_CURRENT_BINARY_DIR}/trace-plot-results.xml,xml")
set_tests_properties(trace_plot PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 90)

# Manual, explicitly requested hardware debugging only; never part of CTest.
add_executable(lin_download_debug lin_download_debug.cpp)
target_link_libraries(lin_download_debug PRIVATE bootloader_ui)
host_deploy(lin_download_debug)

add_executable(test_workbench test_workbench.cpp)
target_link_libraries(test_workbench PRIVATE bootloader_ui Qt${QT_VERSION_MAJOR}::Test)
target_compile_definitions(test_workbench PRIVATE TEST_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
host_deploy(test_workbench)
add_test(NAME workbench COMMAND test_workbench -o "${CMAKE_CURRENT_BINARY_DIR}/workbench-results.xml,xml")
set_tests_properties(workbench PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 90)

# Mock SDK regression; does not load a DLL or access physical devices.
add_executable(test_tosun test_tosun.cpp)
target_link_libraries(test_tosun PRIVATE bootloader_ui Qt${QT_VERSION_MAJOR}::Test)
host_deploy(test_tosun)
add_test(NAME tosun COMMAND test_tosun -o "${CMAKE_CURRENT_BINARY_DIR}/tosun-results.xml,xml")
set_tests_properties(tosun PROPERTIES TIMEOUT 30)
# Explicit operator-controlled hardware probe, never part of CTest.
add_executable(tosun_probe tosun_probe.cpp)
target_link_libraries(tosun_probe PRIVATE bootloader_ui)
host_deploy(tosun_probe)
